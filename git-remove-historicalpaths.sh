#!/usr/bin/env zsh
# commented because this operation can be destructive
echo "uncomment this script to use"
# git filter-branch --force --index-filter \
#   "git rm --cached --ignore-unmatch MY-PATH-TO-DELETE" \
#   --prune-empty --tag-name-filter cat -- --all
